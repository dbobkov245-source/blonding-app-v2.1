# Управление генерацией тестов

## 🔴 Автогенерация тестов временно отключена

Для экономии токенов HuggingFace и ускорения деплоя на Vercel автоматическая генерация AI-тестов была **временно отключена** из процесса сборки.

## 📦 Доступные скрипты сборки

### Обычная сборка (без генерации тестов)
```bash
npm run build
```
Выполняет: `sync-version` → `generate-lessons` → `next build`

### Сборка с генерацией тестов
```bash
npm run build:with-quizzes
```
Выполняет: `sync-version` → `generate-lessons` → `generate-quizzes` → `next build`

## 🔧 Ручная генерация тестов

Если нужно сгенерировать тесты отдельно:

```bash
# Сгенерировать тесты для всех уроков
npm run generate-quizzes

# Пересоздать все тесты заново
npm run generate-quizzes -- --force

# Протестировать генерацию
npm run test-quiz
```

## 📝 Используемые тесты

Приложение продолжает использовать **существующие тесты** из `/public/content/quizzes/`.

Текущие тесты для уроков:
- ✅ Урок 1: 5 вопросов
- ✅ Урок 2: 5 вопросов  
- ✅ Урок 3: 5 вопросов
- ✅ Урок 4: 5 вопросов
- ✅ Урок 5: 5 вопросов
- ✅ Урок 6: 5 вопросов

## 🔄 Возврат к автогенерации

Чтобы вернуть автоматическую генерацию:

1. В `package.json` измените скрипт `build`:
```json
"build": "npm run sync-version && npm run generate-all && next build"
```

2. Или просто используйте:
```bash
npm run build:with-quizzes
```

## ⚙️ Конфигурация

Весь код генерации сохранён в проекте:
- `/scripts/generate-quizzes.js` - скрипт генерации тестов
- `/src/lib/ai.js` - вспомогательные функции для работы с AI

Переменные окружения для HuggingFace:
- `HF_TOKEN` - токен API
- `HF_MODEL_QUIZ` - модель для генерации (по умолчанию: `Qwen/Qwen2.5-72B-Instruct`)
