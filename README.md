# Blonding App v2.1

Интерактивное приложение для обучения техникам блондирования и тонирования волос.

## 🎯 Ключевые возможности

- **3 модуля обучения**: Фундаментальная теория (16 уроков), Блондирование (7 уроков), Тонирование (5 уроков)
- **Автоматическая генерация уроков** из `.docx` файлов
- **Интерактивные тесты** для проверки знаний
- **AI-консультант** на базе Hugging Face
- **Адаптивный дизайн** для мобильных устройств

## 📁 Структура проекта

```
blonding-app-v2.1/
├── lessons/                          # Исходные .docx файлы
│   ├── ФУНДАМЕНТАЛЬНАЯ ТЕОРИЯ.../    # 16 уроков предобучения
│   ├── блондирование/                # 7 уроков блондирования
│   └── тонирование/                  # 5 уроков тонирования
├── public/
│   ├── lessons/                      # Сгенерированные Markdown уроки
│   │   └── index.json                # Индекс всех уроков и модулей
│   └── content/quizzes/              # JSON файлы тестов
├── src/pages/
│   ├── index.tsx                     # Главная — выбор модуля
│   ├── module/[slug].tsx             # Страница модуля — список уроков
│   ├── Theory/[slug].tsx             # Страница урока
│   └── Test/[slug].tsx               # Страница теста
└── scripts/
    └── generate-md.js                # Генератор уроков из .docx
```

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Генерация уроков (если есть новые .docx)
```bash
npm run generate-lessons
```

### 3. Запуск dev-сервера
```bash
npm run dev
```

Откройте http://localhost:3000

## 📚 Модули курса

### 📚 Фундаментальная Теория Колористики (16 уроков)
Предобучение: пигменты, УГТ, цветовой круг, нейтрализация, pH и др.

### 💇‍♀️ Блондирование (7 уроков)
Практика: подготовка, зоны осветления, резистентные волосы, постобработка.

### 🎨 Тонирование (5 уроков)
Практика: выбор красителя, нейтрализация ФО, работа с базами.

## ⚙️ Команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера |
| `npm run build` | Сборка для production |
| `npm run generate-lessons` | Генерация уроков из .docx |

## 🔧 Переменные окружения

| Переменная | Описание |
|------------|----------|
| `HF_TOKEN` | Токен Hugging Face для AI-консультанта |

## 📦 Деплой на Vercel

1. Создайте репозиторий на GitHub
2. Подключите к Vercel
3. Добавьте `HF_TOKEN` в Environment Variables
4. Deploy!

## 📝 Добавление нового урока

1. Добавьте `.docx` файл в соответствующую папку `lessons/`
2. Запустите `npm run generate-lessons`
3. Урок автоматически появится в приложении

## 🧪 Создание теста для урока

Создайте JSON файл `public/content/quizzes/{slug}-quiz.json`:

```json
{
  "slug": "lesson-slug",
  "title": "Название теста",
  "questions": [
    {
      "question": "Вопрос?",
      "options": ["Вариант 1", "Вариант 2", "Вариант 3", "Вариант 4"],
      "correctAnswer": 0,
      "explanation": "Пояснение к ответу"
    }
  ]
}
```

Добавьте запись в `public/content/quizzes/index.json`.

---

**Версия**: 2.1  
**Дата обновления**: 2025-12-10
